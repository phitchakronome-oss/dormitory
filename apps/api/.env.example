# Database connection (PostgreSQL on Docker mapped to port 5433)
DATABASE_URL="postgresql://postgres:password123@localhost:5433/dormitory_db?schema=public"

# Redis connection (Docker mapped to port 6380)
REDIS_HOST="localhost"
REDIS_PORT=6380

# App configuration
PORT=4000
NODE_ENV="development"
